import 'package:flutter/material.dart';


class loginScreen extends StatelessWidget {
  const loginScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('For How Long Do We Have To Suffer From THE SAME THINGS NO MATTER HOW hard you try to handle things to change what hurts you to avoid it keeps fighting you somehow keeps\n poping up excutes the worst cases you thought you have the choice to avoid telling you i\'m always gonna be stronger than you ' ),
      ),
    );
  }
}
